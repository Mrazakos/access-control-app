# Lock API Integration - Architecture & Flow Diagrams

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                         Mobile App                              │
│                                                                 │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐         │
│  │   Devices    │  │    Unlock    │  │     VCs      │         │
│  │   Screen     │  │    Screen    │  │   Screen     │         │
│  └──────┬───────┘  └──────┬───────┘  └──────┬───────┘         │
│         │                  │                  │                 │
│         └──────────────────┼──────────────────┘                 │
│                            │                                    │
│                    ┌───────▼────────┐                           │
│                    │  useLockApi()  │                           │
│                    │     Hook       │                           │
│                    └───────┬────────┘                           │
│                            │                                    │
│                    ┌───────▼──────────┐                         │
│                    │ LockApiService   │                         │
│                    │   (Axios HTTP)   │                         │
│                    └───────┬──────────┘                         │
└────────────────────────────┼────────────────────────────────────┘
                             │
                             │ HTTP REST API
                             │
┌────────────────────────────▼────────────────────────────────────┐
│                      Lock Device API                            │
│                 (http://192.168.0.17:3000/api/v1)              │
│                                                                 │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │ Config Controller│  │ Verify Controller│                    │
│  │   /config/*      │  │    /verify       │                    │
│  └────────┬─────────┘  └────────┬─────────┘                    │
│           │                     │                               │
│  ┌────────▼─────────────────────▼─────────┐                    │
│  │        Lock Configuration Service      │                    │
│  └────────┬───────────────────────────────┘                    │
│           │                                                     │
│  ┌────────▼──────────┐  ┌──────────────────┐                  │
│  │ Blockchain        │  │   Lock Hardware  │                  │
│  │   Listener        │  │    Controller    │                  │
│  └───────────────────┘  └──────────────────┘                  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

## Lock Setup Flow

```
┌─────────────┐
│  Lock Owner │
└──────┬──────┘
       │
       │ 1. Create Lock
       │
       ▼
┌─────────────────┐
│  Mobile App     │
│  (Devices)      │
└──────┬──────────┘
       │
       │ 2. Register on Blockchain
       │
       ▼
┌─────────────────┐
│   Blockchain    │
│   Smart         │──────┐
│   Contract      │      │ Lock registered
└─────────────────┘      │ with ID & Address
       │                 │
       │ 3. Status: Active
       │                 │
       ▼                 │
┌─────────────────┐      │
│  Mobile App     │      │
│  (Setup Button) │      │
└──────┬──────────┘      │
       │                 │
       │ 4. POST /config/init
       │    { lockId, publicKey }
       │                 │
       ▼                 │
┌─────────────────┐      │
│  Lock Device    │      │
│    API          │◄─────┘
└──────┬──────────┘
       │
       │ 5. Store Configuration
       │    Start Blockchain Listener
       │
       ▼
┌─────────────────┐
│  Lock Device    │
│  CONFIGURED ✅  │
└─────────────────┘
```

## Credential Verification & Unlock Flow

```
┌─────────────┐
│  Lock Owner │
└──────┬──────┘
       │
       │ 1. Issue Credential
       │
       ▼
┌─────────────────┐
│  Mobile App     │
│  (VCs Screen)   │
└──────┬──────────┘
       │
       │ 2. Generate QR Code
       │
       ▼
┌─────────────────┐
│   QR Code       │
│   Credential    │
└──────┬──────────┘
       │
       │ 3. Share QR Code
       │
       ▼
┌─────────────────┐
│  User (Visitor) │
└──────┬──────────┘
       │
       │ 4. Scan QR Code
       │
       ▼
┌─────────────────┐
│  Mobile App     │
│  (Unlock)       │
└──────┬──────────┘
       │
       │ 5. Click "Unlock"
       │
       ▼
┌─────────────────┐
│  useLockApi     │
└──────┬──────────┘
       │
       │ 6. POST /verify
       │    { credential }
       │
       ▼
┌─────────────────┐
│  Lock Device    │
│    API          │
└──────┬──────────┘
       │
       │ 7. Verify Signature
       │    Check Revocation
       │    Check Expiration
       │    Check Lock ID
       │
       ▼
┌─────────────────┐
│  Verification   │
│    Result       │
└──────┬──────────┘
       │
       ├─── Valid? ─────────┐
       │                    │
       ▼                    ▼
┌─────────────┐      ┌──────────────┐
│  UNLOCK ✅  │      │  DENY ❌     │
│  Door Opens │      │  Error Msg   │
└─────────────┘      └──────────────┘
```

## Data Flow - Setup Lock

```
Mobile App                  Lock Device API              Blockchain
    │                            │                           │
    │ POST /config/init          │                           │
    ├───────────────────────────>│                           │
    │ { lockId: 1,               │                           │
    │   publicKey: "0x..." }     │                           │
    │                            │                           │
    │                            │ Check if configured       │
    │                            │ (must be first time)      │
    │                            │                           │
    │                            │ Store lockId              │
    │                            │ Store publicKey           │
    │                            │                           │
    │                            │ Start Blockchain          │
    │                            │    Listener               │
    │                            ├──────────────────────────>│
    │                            │ Subscribe to events       │
    │                            │                           │
    │                            │                           │
    │ Response 200 OK            │                           │
    │<───────────────────────────┤                           │
    │ { success: true,           │                           │
    │   lockId: 1,               │                           │
    │   message: "..." }         │                           │
    │                            │                           │
    │ Update UI                  │                           │
    │ (Setup → Reset)            │                           │
    │                            │                           │
```

## Data Flow - Unlock Door

```
Mobile App                  Lock Device API              Blockchain
    │                            │                           │
    │ POST /verify               │                           │
    ├───────────────────────────>│                           │
    │ { @context: [...],         │                           │
    │   id: "vc:...",            │                           │
    │   credentialSubject: {     │                           │
    │     lock: { id: "1" },     │                           │
    │     userMetaDataHash,      │                           │
    │     accessLevel: "standard"│                           │
    │   },                       │                           │
    │   proof: {                 │                           │
    │     proofValue: "0x..."    │                           │
    │   }                        │                           │
    │ }                          │                           │
    │                            │                           │
    │                            │ Extract signature         │
    │                            │ Verify ECDSA signature    │
    │                            │ with stored publicKey     │
    │                            │                           │
    │                            │ Hash credential           │
    │                            │                           │
    │                            │ Check revocation          │
    │                            ├──────────────────────────>│
    │                            │ Query smart contract      │
    │                            │<──────────────────────────┤
    │                            │ isRevoked = false         │
    │                            │                           │
    │                            │ Check expiration          │
    │                            │ Check lock ID             │
    │                            │                           │
    │                            │ All checks pass ✅        │
    │                            │                           │
    │                            │ Actuate lock              │
    │                            │ (open door)               │
    │                            │                           │
    │ Response 200 OK            │                           │
    │<───────────────────────────┤                           │
    │ { verified: true,          │                           │
    │   credentialId: "vc:...",  │                           │
    │   isRevoked: false }       │                           │
    │                            │                           │
    │ Show success message       │                           │
    │ "Door unlocked! 🎉"        │                           │
    │                            │                           │
```

## State Management

```
┌─────────────────────────────────────────────────────────────────┐
│                      useLockApi Hook                            │
│                                                                 │
│  State:                                                         │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ baseUrl: string | null                                  │   │
│  │ isLoading: boolean                                      │   │
│  │ error: string | null                                    │   │
│  │ isHealthy: boolean                                      │   │
│  │ apiService: LockApiService | null                       │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
│  Actions:                                                       │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ setBaseUrl(url)                                         │   │
│  │ clearError()                                            │   │
│  │ initializeLock(lockId, publicKey)                       │   │
│  │ getConfigStatus()                                       │   │
│  │ resetLockConfig(adminCredential)                        │   │
│  │ verifyCredential(credential)                            │   │
│  │ checkHealth()                                           │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
```

## Lock States

```
┌─────────────┐
│  CREATED    │  Lock created in app
└──────┬──────┘
       │
       │ Register on blockchain
       │
       ▼
┌─────────────┐
│  SYNCING    │  Blockchain registration in progress
└──────┬──────┘
       │
       │ Registration complete
       │
       ▼
┌─────────────┐
│  ACTIVE     │  Lock registered on blockchain
└──────┬──────┘
       │
       │ Setup lock device
       │
       ▼
┌─────────────┐
│ CONFIGURED  │  Lock device ready to verify credentials
└─────────────┘
       │
       │ Reset configuration
       │
       ▼
┌─────────────┐
│  ACTIVE     │  Back to active (not configured)
└─────────────┘
```

## Error Handling Flow

```
┌─────────────────┐
│  API Request    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│  Try Request    │
└──────┬──────────┘
       │
       ├─── Success ────────┐
       │                    │
       ▼                    ▼
┌─────────────┐      ┌──────────────┐
│   Error?    │      │  Return Data │
└──────┬──────┘      └──────────────┘
       │
       ├─── Network Error ───┐
       │                     │
       ├─── Timeout ─────────┤
       │                     │
       ├─── 4xx/5xx ─────────┤
       │                     │
       ▼                     ▼
┌─────────────────┐   ┌──────────────────┐
│  Catch Error    │   │  Axios           │
│                 │   │  Interceptor     │
└──────┬──────────┘   └────────┬─────────┘
       │                       │
       │                       │
       ▼                       ▼
┌─────────────────┐   ┌──────────────────┐
│  Set Error      │   │  Log Error       │
│  State          │   │  Console         │
└──────┬──────────┘   └──────────────────┘
       │
       ▼
┌─────────────────┐
│  Show Alert     │
│  to User        │
└─────────────────┘
```

## Security Layers

```
┌─────────────────────────────────────────────────────────────────┐
│                      Security Layers                            │
│                                                                 │
│  Layer 1: Network                                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • Private network (192.168.x.x)                          │  │
│  │ • No internet exposure                                   │  │
│  │ • Timeout protection                                     │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 2: API Endpoints                                         │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • ConfigGuard on /init (once only)                       │  │
│  │ • VcAuthGuard on /reset (admin required)                 │  │
│  │ • VcAuthGuard on /verify (valid VC required)             │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 3: Credential Verification                               │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • ECDSA signature verification                           │  │
│  │ • Blockchain revocation check                            │  │
│  │ • Expiration validation                                  │  │
│  │ • Lock ID matching                                       │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
│  Layer 4: Blockchain                                            │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ • Immutable credential registry                          │  │
│  │ • Revocation list                                        │  │
│  │ • Lock ownership verification                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

**Diagrams Version:** 1.0.0
**Last Updated:** November 6, 2025
